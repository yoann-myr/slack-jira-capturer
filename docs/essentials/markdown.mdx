---
title: "Markdown & MDX"
description: "Standard Markdown plus Mintlify components"
---

## Headings, text, and lists

Standard Markdown works. **Bold**, _italic_, `inline code`, and [links](https://mintlify.com).

## Code blocks

```ts
export function greet(name: string) {
  return `Hello, ${name}`;
}
```

## Callouts

<Note>Use `Note` for helpful side info.</Note>
<Warning>Use `Warning` for things that could break.</Warning>
<Tip>Use `Tip` for shortcuts and recommendations.</Tip>

## Accordions

<AccordionGroup>
  <Accordion title="What is MDX?">
    MDX is Markdown with JSX components mixed in.
  </Accordion>
  <Accordion title="Where do components come from?">
    Mintlify ships a built-in component library — see their docs for the full list.
  </Accordion>
</AccordionGroup>
